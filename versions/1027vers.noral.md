# 版本标记：1027vers.noral（覆盖为当前状态）

- 标识：`1027vers.noral`
- 更新时间：2025-10-27
- 预览地址：`http://localhost:5173/Bingli-project/`
- 基础路径：`/Bingli-project/`
- 接口代理：`/aiChat` → `http://47.106.211.121:8102`

## 变更概览（当前）
- 首页：病例超过一屏时显示“向下滑动查看更多病例”提示（底部居中、淡入出现，滚动到页面底部自动隐藏）。
- 首页：顶部健康管家图片在不与右侧文案重叠的前提下放大，并优化响应式布局。
- 模拟问诊：根据病例内容生成定制化首条患者问候语（不匹配时基于病例标题生成通用问候）。
- 模拟问诊：首条患者问候与每次最新一条病人回复采用逐字展示（约每字符 30ms）。
- 模拟问诊：保留底部灰色三点等待提示（footer），收到回复后自动消失。

## 当前保留项
- 保留 `/aiChat` 代理与“智能提示”模块；未删除任何“测试智能体”相关配置。

## 引用说明
- 版本标识 `1027vers.noral` 已覆盖为当前项目状态，文档、沟通或脚本中可直接引用该标识。
- 若需在代码中程序化读取版本，可从 `versions/1027vers.noral.md` 读取；如需同步到 `package.json` 的自定义字段，我可进一步添加。